@model Instructor;

@{
}

<h1>Add Instructor</h1>
<style>
    span {
        color: red;
    }
</style>

<form action="AddInstructor" method="post">
           @*  <div class="form-group">
                <label asp-for="Id">ID</label>
                <input asp-for="Id" class="form-control">
            </div> *@
            <div class="form-group">
                <label asp-for="FirstName">First Name</label>
                <span asp-validation-for="FirstName"></span>
                <input asp-for="FirstName" class="form-control">
            </div>
            <div class="form-group">
                <label asp-for="LastName">Last Name</label>
                <span asp-validation-for="LastName"></span>
                <input asp-for="LastName" class="form-control">
            </div>
            <div class="form-group">
                <label asp-for="Status">Status</label>
                <span asp-validation-for="Status"></span>
                <select asp-for="Status" class="form-control"><option disabled selected></option>
                    <option value="permanent">Permanent</option>
                    <option value="probationary">Probationary</option>
                </select>
            </div>
            <div class="form-group">
               @* <label asp-for="Rank">Rank</label>
               <span asp-validation-for="Rank"></span>
                <select asp-for="Rank" asp-items="Html.GetEnumSelectList(typeof(Rank))" class="form-control"><option disabled selected></option></select> *@

               @*  <label asp-for="Rank">Rank</label>
                 <span asp-validation-for="Rank"></span>
                  <select asp-for="Rank" asp-items="Html.GetEnumSelectList(typeof(Rank))" class="form-control" id="rankSelect">
                    <option value="">Select Rank</option>
                     @foreach (var item in Html.GetEnumSelectList(typeof(Rank)))
                     {
                      <option value="@item.Value">@item.Text</option>
                     }
                   </select> *@

                  <label asp-for="Rank">Rank</label>
                  <span asp-validation-for="Rank"></span>
                  <select asp-for="Rank" asp-items="Html.GetEnumSelectList(typeof(Rank))" class="form-control">
                  </select>
            </div>
            <div class="form-group">
                <label asp-for="HiringDate">Hiring Date</label>
                <span asp-validation-for="HiringDate"></span>
                <input asp-for="HiringDate" class="form-control" type="date">
            </div>

        <button type="submit" class="btn btn-primary">Submit</button>
</form>

